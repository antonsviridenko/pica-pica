# Defaults for pica-node initscript
# sourced by /etc/init.d/pica-node
# installed at /etc/default/pica-node by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="-v"
