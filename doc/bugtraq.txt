1. [V] Падение test_client при вызове SSL_free:

puts("CACA_close_channel_chkp3");//debug

if (chn->ssl)
SSL_free(chn->ssl);<<<<

после 
verify result:10  // просроченный корневой сертификат
////////////
причина - повторный вызов SSL_free
(сначала в функции establish_data_connection, затем в CACA_close_channel)

Баг исправлен путем помещения вызова CACA_close_channel в establish_data_connection в случае ошибки при создании канала. Также добавлена новая callback функция channel_failed, вызываемая в случае ошибки при создании канала

==========================================================
2. Авторизация клиента узлом без сертификата

Клиент возможно может установить TLS соединение с узлом используя ADH схему. Узел 
может выпасть с ошибкой, если не удается получить сертификат клиента. Надо явно задавать 
набор разрешенных схем шифрования в клиенте и сервере
